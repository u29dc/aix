{
	"name": "aix",
	"version": "1.0.0",
	"type": "module",
	"private": true,
	"scripts": {
		"init---------------------------------": "",
		"prepare": "husky",
		"dev----------------------------------": "",
		"dev": "bun build src/index.ts --outfile dist/contentScript.js --target browser --watch",
		"build": "bun build src/index.ts --outfile dist/contentScript.js --target browser && terser dist/contentScript.js -o dist/contentScript.js -c passes=3,dead_code=true,drop_console=true,drop_debugger=true -m toplevel=true && cp manifest.json dist/ && cp -r assets dist/",
		"check--------------------------------": "",
		"util:format": "biome format --write .",
		"util:lint": "biome check . --max-diagnostics 500",
		"util:lint:fix": "biome check --write .",
		"util:types": "tsgo --noEmit",
		"util:check": "STATUS=0; bun run util:format || STATUS=1; bun run util:lint || STATUS=1; bun run util:types || STATUS=1; bun test || STATUS=1; exit $STATUS"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"@commitlint/cli": "^20.3.1",
		"@commitlint/config-conventional": "^20.3.1",
		"@happy-dom/global-registrator": "^20.3.1",
		"@typescript/native-preview": "^7.0.0-dev.20260118.1",
		"bun-types": "^1.3.6",
		"husky": "^9.1.7",
		"lint-staged": "^16.2.7",
		"terser": "^5.46.0",
		"typescript": "^5.9.3"
	}
}
